spring:
  datasource:
    url: jdbc:mysql://localhost:3306/security
    username: root
    password: marcos
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  security:
    user:
      name: user
      password: user
      roles: USER